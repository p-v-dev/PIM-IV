# Swagger Implementation Plan

**Goal:** Publish interactive API documentation at `/api`.

- [ ] Install Nest Swagger dependencies.
- [ ] Configure Swagger and Bearer authentication in `src/main.ts`.
- [ ] Add minimal endpoint and DTO annotations.
- [ ] Run tests, lint, and build.
