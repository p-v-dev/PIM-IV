# Swagger Design

Swagger is public at `/api`. It documents all current endpoints, request DTOs, and JWT Bearer authentication. The UI uses the standard Nest Swagger setup without custom pages or manual OpenAPI files.
